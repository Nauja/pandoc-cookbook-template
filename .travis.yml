before_install:
- sudo apt-get update -qq
- sudo apt-get install -y pandoc
- sudo apt-get install -y texlive-full
- sudo apt-get install -y texlive-xetex

script:
  - pandoc -o example/melon-pan.html --template template.html --css template.css --metadata image=example/melon-pan.jpg --self-contained example/melon-pan.yaml
